import SiteBuilderContainer from '@/SiteBuilderContainer'
import React from 'react'
import ReactDOM from 'react-dom/client'

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <SiteBuilderContainer />
  </React.StrictMode>
)
